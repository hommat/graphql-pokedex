import React, { Fragment } from 'react';

import Form from '../components/home/Form';

const HomePage = () => {
  return (
    <Fragment>
      <Form />
    </Fragment>
  );
};

export default HomePage;
